# pytest.ini
[pytest]
asyncio_mode=auto

asyncio_default_fixture_loop_scope = function

markers =
    slow: marks tests as slow (deselect with '-m \"not slow\"')
    asyncio: marks tests that use asyncio
    integration: marks tests that require a running DICOMWeb service
